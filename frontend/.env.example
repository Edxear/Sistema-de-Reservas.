# Frontend mode
# true  -> no backend required (all sections visible)
# false -> real auth/API required
REACT_APP_DEMO_MODE=true

# Backend URL only used when REACT_APP_DEMO_MODE=false
REACT_APP_API_URL=http://localhost:5000
